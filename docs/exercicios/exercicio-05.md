# Exercícios - Aula 05: Funções

Modularizando o código para reutilização.

### 🟢 Básicos
1. **Função Simples**: Crie uma função que receba um nome e imprima "Olá, [nome]! Seja bem-vindo.".
2. **Retorno**: Implemente uma função que receba dois números e retorne o maior deles.

### 🟡 Intermediários
3. **Passagem por Referência**: Crie uma função `void quadrado(int &n)` que altere o valor da variável original para o seu quadrado.
4. **Sobrecarga**: Crie duas funções chamadas `area()`. Uma deve calcular a área de um quadrado (1 parâmetro) e a outra a área de um retângulo (2 parâmetros).

### 🔴 Desafio
5. **Recursividade**: Crie uma função recursiva que calcule a soma de todos os números de 1 até N (fornecido pelo usuário).